
@model List<Shoping_Site.Models.Secundarias.Articulo>

<div class="contenedorPrimario" id="pagInicio">
    <section class="navBar" id="navBar">
        <ul>
            <li id="etiquetasMenu">
                <a>@Html.ActionLink("Tienda", "Index", "Tienda", new { area = "" }, new { @class = "navbar-brand" })</a>
            </li>
            <li id="etiquetasMenu">
                <a> @Html.ActionLink("Carrito", "Carrito", "Tienda", new { area = "" }, new { @class = "navbar-brand" })</a>
            </li>
            <li id="etiquetasMenu">
                <a>@Html.ActionLink("Ingresar", "Index", "Login", new { area = "" }, new { @class = "navbar-brand" })</a>
            </li>
        </ul>
    </section>
</div>

<section id="conetenedor">
    <section class="carrito" id="comun">
        <br><br><h3>Mi Carrito de Compras</h3><br>
        <ul>
            @foreach (var item in Model)
            {
                <li id=@item.ID >
                    <form action="../Tienda/EliminarArticuloCarrito" method="POST">
                        <p>Nombre: @item.Nombre</p>
                        <p>Precio: @item.Precio</p>
                        <img src=@item.Ima id="imagenVenta" style="height: 130px;">
                        <!-- elemento oculto para el paso de datos -->
                        <input id="oculto" name="idArticulo" type="text" value=@item.ID />
                        <p>Cantidad @item.Cantidad</p>
                        <p style="margin-right: 10%"><input onclick="alert('Su petición esta siendo ejecutada')" id="boton" type="submit" value="Editar Cantidad"  /></p>
                        <input id="botonCentrado" type="number" value=@item.Cantidad min="0" max=@item.CantidadMaxima name="cantidad" />
                    </form>
                </li>
            }
        </ul>
    </section>

    <section>
        <form action="../Tienda/hacerPago" method="POST" id="botonesCarrito">
            <p><input id="botonCentrado" type="submit" value="Procesar Compra" /></p><br>
            <h3 style="color: white">Monto: @ViewBag.precioTotal Colones</h3>
        </form>
    </section>

</section>
